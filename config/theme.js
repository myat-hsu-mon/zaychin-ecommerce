import { theme } from '@chakra-ui/core'

export default {
  ...theme,
  fonts: {
    ...theme.fonts,
    // heading: '"Avenir Next", sans-serif',
    body: "Padauk, system-ui, sans-serif",
    mono: "Menlo, monospace",
  },
}
